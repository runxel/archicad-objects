!--- Master script ---!! TODO:! 	- number + gap given => width?!	- width + gap given  => number?!	- nice UI!	- make endpoints Z movable in 3D_ptm = GLOB_SCALE / 1000  	! Paper to Modelmarker_size = min(A/5, 0.7)c_size = min(width_obj/3, gap, A/40, 0.25)! Number of Objects + Width of Objects givengap_total = A - (n_obj * width_obj)gap = gap_total / (n_obj+1)parameters gap = gap